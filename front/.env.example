# API設定
# バックエンドAPIを使用する場合はtrueに設定
VITE_USE_API=false

# バックエンドAPIのベースURL
# バックエンドが稼働している場合はURLを設定
VITE_API_URL=http://localhost:8080

# 使い方:
# 1. このファイルを .env にコピー: cp .env.example .env
# 2. 開発中は VITE_USE_API=false のまま（静的データを使用）
# 3. バックエンド統合時は VITE_USE_API=true に変更
